import Link from 'next/link';
export default function PropertyCard({p}:any){return <article className="bg-white rounded-2xl overflow-hidden shadow-sm border"><img src={p.image} alt={p.title} className="w-full h-56 object-cover"/><div className="p-5"><div className="flex justify-between text-xs uppercase tracking-wider"><span>{p.operation} · {p.type}</span>{p.featured&&<span className="gold font-semibold">Destacada</span>}</div><h3 className="text-xl font-semibold mt-2">{p.title}</h3><p className="text-sm text-slate-500 mt-1">{p.location}</p><p className="text-2xl font-bold mt-4">{p.currency==='USD'?'US$':'RD$'} {p.price.toLocaleString()}</p><div className="text-sm text-slate-500 mt-3">{p.beds} hab. · {p.baths} baños · {p.parking} parqueos · {p.area} m²</div><Link href={'/propiedades/'+p.id.toLowerCase()} className="inline-block mt-5 border border-slate-300 rounded px-4 py-2 text-sm">Ver propiedad</Link></div></article>}
